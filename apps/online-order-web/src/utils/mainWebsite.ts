/**
 * Blade marketing site root — same host, outside the /order SPA basename.
 * Prefer a plain <a href> (not React Router Link) so the browser leaves the app.
 */
export const MAIN_WEBSITE_HREF = '/';
