// All shared types live in packages/shared — re-export from there.
export type {
  Modifier,
  MenuItem as Item,
  Category,
  CartItem,
  RestaurantTable,
  StaffLoginResponse,
  StaffUser,
  Order,
  OrderItem,
  Payment,
} from '@shared/types';
