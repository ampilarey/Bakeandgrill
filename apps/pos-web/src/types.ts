// All shared types live in packages/shared — re-export from there.
export type {
  Modifier,
  MenuItem as Item,
  Variant,
  Category,
  CartItem,
  PackagingOption,
  RestaurantTable,
  StaffLoginResponse,
  StaffUser,
  Order,
  OrderItem,
  Payment,
} from '@shared/types';
